UI ELEMENT: This is to show that part 2.1, part 2.2, part 2.3, part 2.4, and part 2.5 are functional. The user is given given 3 options, option 1 is the naviation option this option will show that parts 2.1 and 2.2 are functional, there are 2 options within the naviation option.The first option within naviation option will get the fastet route between two campus locations, this option will rely on user input, the user will input where they and also input where they wish to go. The shortest route is then calculated using Dijkstra's algorithm. The second option in the navigation option will get the history of the user's movement, allowing the user to go backwards in their route. This option 1 was grouped together because navigation is broadly aligned. Option 2 is for room and event bookings. This part of the program allows the user to manage reservations for campus rooms. When the user enters the booking menu, they are given several different actions related to creating, removing, and viewing bookings. To add a booking, the user first enters the name of the building they want to book in. The system checks whether that building name is valid and then displays the available rooms in that building. The user then selects a room and provides the date, start time, end time, event name, and organizer name. Once this information is entered, the booking is created and stored. The booking menu also allows the user to remove an existing booking by entering its booking ID and room ID. In addition, the user can view a specific booking by entering its booking ID, or view all bookings currently stored in the system. For searching purposes, the user can look up bookings for a specific date, or narrow the results further by providing both a date and a time range. There is also an option to display the next upcoming booking, which allows the user to quickly see which reservation is scheduled to occur next. Overall, this option demonstrates that the booking system is able to create, manage, search, and display room reservations in a structured way. Option 3 is for service requests. This allows the user to report issues around campus and assign a level of urgency to them. When the user selects this option, they are prompted to describe the problem they are experiencing, for example something like a broken projector or a power outage. After entering the description, the user is then asked to input the severity of the issue as either low, medium, or high. This severity is mapped internally to a priority value, where high priority requests are handled before medium and low priority ones. Once both the description and severity are provided, the request is stored in a priority queue. This queue ensures that more urgent issues are processed first rather than strictly in the order they were received. Within this same menu, the user also has the option to view the current service queue. This allows them to see all pending requests along with their priority levels. This demonstrates that the service request system is functioning correctly, as requests are being stored and organized based on urgency.

Part 2.6 No UI element since it doesn't make sense to have this be user driven, but there is a way to test and check for correctness there are 20 (as per the instructions) sequential requests being processed in order.

Part of this md was written with the help of generative AI.